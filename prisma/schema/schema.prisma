generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder", "multiSchema"]
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:root@localhost:5432/contact-footer"
  schemas  = ["auth", "content", "footer"]
}